src/
├── App.vue                              # Layout + router manual (/  y  /<id>)
├── main.ts                              # Bootstrap de Vue (createApp + style.css)
├── style.css                            # Tailwind v4 + tokens dark mode + scrollbar
├── assets/                              # Imágenes estáticas (hero, vite, vue)
├── components/
│   ├── Sidebar.vue                      # Barra lateral fija + drawer responsive
│   ├── icons/
│   │   ├── IconChat.vue
│   │   ├── IconDashboard.vue
│   │   ├── IconMenu.vue
│   │   └── IconUpload.vue
│   └── views/
│       ├── DashboardView.vue            # Kanban semáforo + KPIs + loading/error
│       ├── SiniestroCard.vue            # <a target="_blank"> a /<id_siniestro>
│       ├── SiniestroDetailView.vue      # Detalle completo en ruta /<id>
│       ├── UploadView.vue               # Drag & drop CSV → uploadCsv()
│       └── ChatView.vue                 # Chat → sendChatMessage() + HTML sanitizado
├── data/
│   └── dashboard.json                   # Mock de Siniestro[] (alineado con la SQL)
├── services/
│   ├── api.ts                           # apiUrl + readError (helpers de fetch)
│   ├── siniestros.ts                    # fetchSiniestros + fetchSiniestroById
│   ├── upload.ts                        # uploadCsv
│   └── chat.ts                          # sendChatMessage
├── types/
│   └── siniestro.ts                     # Tipos + REGLA_LABELS + ALERTA_LABELS
├── utils/
│   └── sanitize.ts                      # DOMPurify wrapper para v-html del chat
├── resources/
│   ├── create_siniestros_scored_final.sql  # DDL real de la tabla del backend
│   └── plantilla.png                    # Referencia visual del kanban
├── resources.txt                        # Prompt replicable del sistema
└── structure.txt                        # Este archivo
